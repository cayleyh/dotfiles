## Homebrew-first path setup
PATH=/usr/local/bin:/usr/local/sbin:${PATH}

## Fastlane
#PATH="${PATH}:${HOME}/.fastlane/bin"

## Android
# Android SDK Path is not needed if installed via Brew
# If installed via Android Studio, these are required:
export ANDROID_HOME=/Users/$USER/Library/Android/sdk
PATH=${PATH}:$ANDROID_HOME/tools:$ANDROID_HOME/platform-tools

# Go binaries
# (Homebrew-based Go install)
PATH=${PATH}:/usr/local/opt/go/libexec/bin

# Python3 Related Dependencies
# - As directed by `brew install python`


######
export PATH
